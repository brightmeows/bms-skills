# BMS memo markdownlint 配置
#
# 以下禁用规则均因 BMS 技术文档特例：
# - #WAV01 / #PLAYER 是 BMS 语法在代码块中被误判为标题 (MD025)
# - 表格内 <pre> 代码块无法统一 fenced/indented (MD046)
# - <pre> 嵌入导致管道符/列数误报 (MD055/MD056)
# - CJK+代码混排表格列对齐误报 (MD060)

default = true
MD025 = false   # #WAV01 / #PLAYER 是 BMS 语法，非标题
MD046 = false   # 表格内 <pre> 代码块无法统一 fenced/indented
MD055 = false   # 表格内 <pre> 嵌入导致管道符风格不一致
MD056 = false   # 同上，列数误报
MD060 = false   # CJK+代码混排表格中列对齐误报严重

[MD013]
line_length = 120   # 行宽 120，技术文档够用
tables = false      # 表格行内代码示例无法折行

[MD033]
allowed_elements = ["br", "code", "pre", "samples"]  # 表格内嵌代码 + BMS <samples> 语法
